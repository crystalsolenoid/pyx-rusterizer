pub struct Buffer {
    pub pixels: Vec<u8>,
    pub width: usize,
    pub height: usize,
    pub scale: usize,
    pub palette: [u32; 4],
}

impl Buffer {
//    pub fn new
    pub fn pix(x: usize, y: usize, color: usize) {

    }
}
